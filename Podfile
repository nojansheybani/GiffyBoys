# Uncomment the next line to define a global platform for your 
platform :ios, '9.0'

target 'gifMeSomeLovin' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for gifMeSomeLovin
    pod 'GiphyCoreSDK'
    pod 'FLAnimatedImage'

end
